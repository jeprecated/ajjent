# Use a built-in TUI instead of fzf

`jjw` will use a built-in Go TUI for interactive Workspace flows rather than an `fzf`-based selector. Although `fzf` is convenient for fuzzy picking, the redesigned UX needs colored status badges, disabled rows, mode toggles, and command-specific footer controls that are clearer to own directly in the application.
